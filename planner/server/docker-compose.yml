services:
  planer:
    build:
      context: ../..
      dockerfile: planner/server/Dockerfile
    env_file:
      - .env
    ports:
      - "127.0.0.1:8787:8787"
    volumes:
      - planer_data:/data
    restart: unless-stopped

volumes:
  planer_data:
